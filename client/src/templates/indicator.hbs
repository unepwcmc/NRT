{{#each sections}}
  {{addSubViewTo ../thisView "SectionView" section=this}}
{{/each}}

{{#if isEditable}}
  <button class="add-indicator-section">
    <i class="icon-plus append"></i>
  </button>
{{/if}}
