# Take response from a single poll
# Find dates in `dateField`
# Get earliest and latest dates
# Create array of dates which are `pollFrequency`
# For operation == 'sum' do X, for operation == 'count' do Y
# Store result



request = require('request')


exports.result = []
